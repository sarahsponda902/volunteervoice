<p style="font-size:3em; line-height:2em; margin-top:20px;padding-bottom:20px;text-align:center; ">Listing Messages</h1>

<table>
  <tr>
    <th>Sender</th>
	<th>Recipient</th>
  </tr>

<% @messages.each do |f| %>

  <tr>
	<td><%=User.find(f.sender_id).username%></td>
	<td><%=User.find(f.recipient_id).username%></td>
  </tr>

<% end %>

</table>
