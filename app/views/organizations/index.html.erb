
<p style="font-size:3em; text-align:center">Listing organizations</p><br />

<%= link_to 'Create New Organization', new_organization_path %>


<table>
  <tr>
     <th>Name</th>
	<th># of Programs</th>
    <th>Description</th>
     <th>Image</th>
    <th>Delete?</th>
    <th>Edit?</th>
  </tr>

<% @organizations.each do |organization| %>
  <tr>
  	<td><%= link_to organization.name, organization %></td>
	<td><%= organization.programs.count %></td>
    <td><%= organization.description.html_safe %></td>
	<td><%= image_tag organization.square_image.url, :style => "height:100px;width:100px" %></td>
	<td><%= link_to "Delete", :controller => "organizations", :action => "destroy", :id => organization.id %></td>
	<td><%= link_to "Edit", edit_organization_path(organization) %></td>
  </tr>
<% end %>

</table>
