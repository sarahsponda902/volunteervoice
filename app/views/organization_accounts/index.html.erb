<h1>Listing organization_accounts</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @organization_accounts.each do |organization_account| %>
  <tr>
    <td><%= link_to 'Show', organization_account %></td>
    <td><%= link_to 'Edit', edit_organization_account_path(organization_account) %></td>
    <td><%= link_to 'Destroy', organization_account, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Organization account', new_organization_account_path %>
